name: aplicativo_firebaseremoteconfig
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  firebase_core: ^4.10.0
  firebase_remote_config: ^6.5.2
  flutter:
    sdk: flutter
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/propaganda.png
    - assets/images/propaganda_alt.png
